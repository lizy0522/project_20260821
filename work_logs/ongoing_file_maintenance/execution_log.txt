持续维护模式记录
================

时间：2026-08-21 19:39:13 +08:00

维护对象：
D:Project_Filespython_projectproject_20260821AGENTS.md

维护目的：
将工程工作流从“每次修改都必须新建任务”调整为双轨制：
独立分析或结果产出继续使用递增任务编号；固定文件的连续修改复用本维护记录，
不新建任务目录，也不消耗新的任务编号。

本次修改：

- 新增“新任务模式”和“持续维护模式”；
- 规定固定文件直接在原路径修改；
- 固定使用 work_logs/ongoing_file_maintenance/execution_log.txt 记录连续维护；
- 持续维护摘要追加到 work_logs/codex_handoff/codex_handoff.txt；
- 明确只有产生独立脚本、数据、模型或结果时，才切回新任务模式；
- 将完成前检查改为同时覆盖两种工作模式。

验证结果：

- AGENTS.md 已写入双轨工作模式规则；
- 未创建新的编号任务；
- 未修改 data、results 或 scripts 中的业务文件；
- 工程当前 Git 工作区待提交本次维护变更。

后续维护要求：

后续用户明确指定的固定文件修改，直接追加到本文件；
每次记录时间、文件路径、修改目的、变更内容、验证结果和未完成事项。
